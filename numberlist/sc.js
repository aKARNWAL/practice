const btn = document.getElementsByTagName('button')[0]
const inp = document.getElementById('mynum')
// console.log(document.getElementById('mynum'))
const list = document.getElementById('item');
// const a = document.getElementById('abc')

btn.addEventListener('click', () => {

    console.log("th",inp)
    // a.innerText = inp.value;
        so.innerText= inp.value;
        so.textContent = "Hello, I'm a paragraph.";

// Add new text and paragraph inside the box
so.append(" ← Text bhi", inp.value);
   
console.log("th")

    // const list = document.getElementById("");
    // const li = document.createElement("");
    //     li.innerText = numbers[currentIndex];

    //     // Add to UI
    //     list.appendChild(li);

    //     currentIndex++; // Move to next number
})
